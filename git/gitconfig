[user]
  name = Danielle Sucher
  email =  dsucher@gmail.com
  website = http://daniellesucher.com
[alias]
  a = add
  ci = commit
  co = checkout
  chp = cherry-pick
  d = diff
  f = fetch
  fo = fetch origin
  p = push
  pom = push origin master
  pos = push origin staging
  r = rebase
  rb = rebase
  s = status
  # the next few aliases are are from http://blog.apiaxle.com/post/handy-git-tips-to-stop-you-getting-fired/
  assume = update-index --assume-unchanged
  unassume = update-index --no-assume-unchanged
  assumed = "!git ls-files -v | grep ^h | cut -c 3-"
[github]
  user = DanielleSucher
  token =   #fill in token
[core]
  excludesfile = ~/.gitignore_global
[push]
  default = current
[color]
  diff = auto
  status = auto
  branch = auto
[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green
[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red
  new = cyan
[color "status"]
  added = yellow
  changed = green
  untracked = cyan
[diff]
  algorithm = patience
